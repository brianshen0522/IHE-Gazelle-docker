---
page.title: Gazelle User Management
---

# Gazelle User Management

This project is used for managing Gazelle users.

## Prerequisites

- PostgreSQL 10+ with `max_prepared_transactions` configuration (postgresql.conf) higher than 0. 
Recommended equals to `max_connections`.